node_modules
dist
dist-ssr
*.local
.DS_Store
image-search
.npm
.cache
.vscode/*
!.vscode/extensions.json
.idea
server/data/messages.json
server/uploads/*
!server/uploads/.gitkeep
server/data/shop.json

# SQLite database (runtime data: orders, comments, messages, shop products)
server/data/*.db
server/data/*.db-wal
server/data/*.db-shm


# Secrets — ADMIN_PASS / ADMIN_SECRET live here, never in the repository
.env
.env.*
!.env.example
